namespace MonsterTamer.Party.Enums
{
    internal enum PartyMenuState
    {
        Selection, // default browsing
        Options,   // action menu open
        Swap,      // choosing swap target
        Item       // selecting item target
    }
}
